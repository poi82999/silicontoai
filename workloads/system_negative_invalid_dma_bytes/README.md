# system_negative_invalid_dma_bytes

This package is intentionally invalid.

Its purpose is to prove that the system replay runner rejects contradictory system DMA metadata as a package error before replaying the DUT.